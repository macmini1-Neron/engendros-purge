# Money gun (Lukova fáze 3 + drop pro hráče)

- `money-gun.js` — model `buildMoneyGun(b, x, y, z, s)` (MeshBuilder, předek = −Z).
  Pistolový tvar, vepředu kvádr s koukající měděnou mincí. Munice = měděné mince.
- `images/` — sem dej referenční obrázek, podle kterého model doladíme.
